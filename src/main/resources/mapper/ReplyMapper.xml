<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.library.mapper.ReplyMapper">
	<!-- 게시글별 댓글 목록 조회 -->
	<select id="getAllRepliesByArticlesId" resultType="com.library.model.Reply" parameterType="int">
		SELECT * FROM REPLIES WHERE ORIGIN_ARTICLE_ID = #{articlesId}
	</select>
	
	<!-- 댓글 등록 -->
	
	<!-- 댓글 수정 -->
	
	<!-- 댓글 삭제 -->

</mapper>