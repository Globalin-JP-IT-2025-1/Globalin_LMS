<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.library.mapper.ReplyMapper">
	<!-- 게시글별 댓글 목록 조회 -->
	<select id="getAllRepliesByArticlesId" resultType="com.library.model.Reply" parameterType="int">
		SELECT * FROM REPLIES WHERE ORIGIN_ARTICLE_ID = #{articlesId}
	</select>
	
	<!-- 댓글 수정 -->
	<!-- 1) 정보 수정 : title, content -->
	<update id="updateReplyInfo" parameterType="com.library.model.Reply">
        UPDATE REPLIES 
        SET	CONTENT = #{content}
	    WHERE REPLIES_ID = #{repliesId}
    </update>
    
    <!-- 2) 비공개 댓글로 전환 : status -->
	<update id="updateReplyDisable" parameterType="com.library.model.Reply">
        UPDATE REPLIES 
        SET	STATUS = 2 
	    WHERE REPLIES_ID = #{repliesId}
    </update>
    
    <!-- 3) 공개 댓글로 전환 : status -->
    <update id="updateReplyEnable" parameterType="int">
        UPDATE REPLIES 
        SET	STATUS = 0
	    WHERE REPLIES_ID = #{repliesId}
    </update>
    
    <!-- 4) 비밀 댓글로 전환 : status -->
    <update id="updateReplySecret" parameterType="int">
        UPDATE REPLIES 
        SET	STATUS = 1
	    WHERE REPLIES_ID = #{repliesId}
    </update>
	
	<!-- 댓글 등록 -->
	<insert id="insertReply" parameterType="com.library.model.Reply">
	    <selectKey keyProperty="repliesId" resultType="java.lang.Integer" order="BEFORE">
	        SELECT SQ_REPLIES.NEXTVAL FROM DUAL
	    </selectKey>
        INSERT INTO REPLIES (
		    REPLIES_ID, ORIGIN_ARTICLE_ID, AUTHOR_ID, 
		   	CONTENT, CREATE_DATE, UPDATE_DATE, STATUS
		) VALUES (
		    #{repliesId}, #{originArticleId}, #{authorId}, 
		    #{content}, #{createDate}, #{updateDate}, #{status})
    </insert>
	
	<!-- 댓글 삭제 -->
	<delete id="deleteReply" parameterType="int">
        DELETE FROM REPLIES WHERE REPLIES_ID = #{repliesId}
    </delete>

</mapper>